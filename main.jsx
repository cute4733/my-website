import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx' // 確保名稱與新檔案完全一致
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)